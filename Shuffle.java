/** * File: Shuffle.java * Author: Tamsin Rogers * Date: 2/11/20 */	import java.util.ArrayList;import java.util.Random;public class Shuffle{	public static void main ( String[] args )		{ 		ArrayList<Integer> arr = new ArrayList<Integer>();		//array list object		ArrayList<Integer> removed = new ArrayList<Integer>();		Random ran = new Random();								//random object			/*		for (int i=0; i<10; i++)		{			int randomint = ran.nextInt(100);			arr.add(randomint);		}		*/			for (int i=1; i<11; i++)		{			arr.add(i);		}			System.out.println(arr + ", ");			for (int i=0; i<10; i++)		{			int length = arr.size();			for (i = 0; i<10; i++)			{				int current = arr.get(i);				int randomIndex = i + ran.nextInt(10-i);				arr.set(i, arr.get(randomIndex));				arr.set(randomIndex, current);			}		}			System.out.println("shuffled: " + arr + ", ");			for (int i=0; i<10; i++)		{			Integer x = arr.get(i);			System.out.println(x);		}			for (int i=0; i<10; i++)		{			int length = arr.size();			int randomindex = ran.nextInt(length);			//chooses a random index in the list			Integer x = arr.get(randomindex);			arr.remove(x);			removed.add(x);			System.out.println("removed: " + x + ", " + "remaining values: " + arr + " removed values: " + removed);		}	}}